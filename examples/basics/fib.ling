#!/usr/bin/env ling

ผูก fib = (n: number) -> number {
    ถ้า n <= 1 {
        post n
    } มิฉะนั้น {
        post fib(n - 1) + fib(n - 2)
    }
}

ผูก start = ทำ {
    สำหรับ i ใน 0..10 {
        พิมพ์("fib(", i, ") = ", fib(i))
    }
}
